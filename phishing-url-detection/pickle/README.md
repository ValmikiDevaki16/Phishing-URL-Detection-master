# This folder stores the trained ML model (model.pkl)
# model.pkl is excluded from git (.gitignore) due to file size and version compatibility.
# Run retrain.py to generate model.pkl for your environment.
